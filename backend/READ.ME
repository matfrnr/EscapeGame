Créer une nouvelle table :

npx knex migrate:make create_table_name

Modifier la dernière migration :

Lancer la migration :
npx knex migrate:latest

-- Modifier une table déjà existante --

npx knex migrate:make modify_table_name

Modifier la dernière migration :

Lancer la migration :
npx knex migrate:latest

-- -- --

Sur serveur utiliser /api pour acceder au routes du backend

Problème mac : port 3306 déjà utilisé, vérifier avec : lsof -i :3306
Fermer brew my sql avec : brew services stop mysql

Pour le jeu d'echec :

npm install @babel/runtime
npm install chess.js react-chessboard